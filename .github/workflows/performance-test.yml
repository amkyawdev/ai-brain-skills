name: Performance Tests

on:
  push:
    branches: [main]
  schedule:
    - cron: '0 0 * * 0'

jobs:
  performance:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      
      - name: Run performance tests
        run: bash scripts/performance/measure-execution.sh
